import { BtnRoundDirective } from './btn-round.directive';

describe('BtnRoundDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnRoundDirective();
    expect(directive).toBeTruthy();
  });
});
